# @package _global_
defaults:
  - override /dataset: dnn.yaml
  - override /model: dnn.yaml
  - _self_


model:
  input_dim: 2
  hidden_dim: 20
  output_dim: 1
  n_layers: 8
  activation: lrelu

dataset:
  idx: 0
  num_data: 100000