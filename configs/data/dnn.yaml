_target_: src.data.burgers_datamodule.BurgersDataModule
batch_size: 128
train_val_test_split: [0.7, 0.2, 0.1]
num_workers: 0
pin_memory: False

dataset: 
  _target_: src.data.components.datasets.DNNDataset
  data_dir: ${paths.data_dir}
  filename: ${filename}
  idx: 0 # index of instance to train
  num_data: 10000 # number of data to use